# dslib
`dslib` is a data structures' library that implements most of the concepts from **Intro to Computing II** (ITI1121) and **Engineering Computation** (GNG1106) and at the University of Ottawa. This project dives deeper into custom structured data, generics, and the fundamentals of memory management using C Programming Language. The goal of this project was to learn and apply C programming language from GNG1106 and from self-learning by understanding **memory allocations, efficient memory management, memory leaks, critical debugging, and custom generics** using `void*` to make the data structures library universal. 

# Tasks to Complete
- Ensure all lines of code do not leave any memory leaks 
- Providing Documentation on each .c and Makefile files
- Provide debugging lines to label where I keep landing on the same problem

